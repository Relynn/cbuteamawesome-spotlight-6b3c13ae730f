<%= form_for(@presentation_block) do |f| %>
  <% if @presentation_block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@presentation_block.errors.count, "error") %> prohibited this presentation_block from being saved:</h2>

      <ul>
      <% @presentation_block.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
